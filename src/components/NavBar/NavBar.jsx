/** @format */

export function NavBar() {
  return (
    <div>
      Navigation Bar
      {/* <logo />
        <div>
        {isLoggedIn ? 
            <avatar />
            <username />
            <exit/>
            : ""}
        </div> */}
    </div>
  );
}
